import React from 'react';
import { FragmentTransitions } from '../../types/reveal.js';
import { SimpleComponent } from './BaseComponent.js';
export interface FragmentContextType {
    setNotes: (notes: string) => void;
    index?: number;
    transition?: FragmentTransitions;
}
export declare const fragmentContext: React.Context<FragmentContextType>;
export interface FragmentProps {
    index?: number;
}
export interface FragmentProps {
    index?: number;
    transition?: FragmentTransitions;
    children: React.ReactNode;
    tag?: SimpleComponent;
}
export default function Fragment({ index, transition, children, tag, }: FragmentProps): JSX.Element;
