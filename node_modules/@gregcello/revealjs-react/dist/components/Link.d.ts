import React from 'react';
import { MakeProps } from './BaseComponent';
export declare type SlideType = string | number | [number, number] | HTMLElement;
export interface LinkProps {
    children: React.ReactNode;
    href?: string;
    slide?: SlideType;
    autoAnimateId?: string;
}
declare function Link({ autoAnimateId, children, fragmentIndex, href, slide, ...props }: MakeProps<LinkProps, 'a'>): JSX.Element;
export default Link;
