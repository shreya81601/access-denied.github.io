import React from 'react';
import './Example.css';
export interface ExampleProps {
    source?: string;
    autoAnimateId?: string;
    children: React.ReactNode;
}
export default function Example({ source: code, autoAnimateId, children, }: ExampleProps): JSX.Element;
