/// <reference types="react" />
import { MakeProps } from './BaseComponent';
export interface CodeProps {
    children: {
        code: string;
    };
    language?: string;
    escape?: boolean;
    lineNumbers?: string | true;
    startLineNumbersAt?: number;
    noTrim?: boolean;
    autoAnimateId?: string;
}
declare function Code({ id, children, language, escape, fragmentIndex, lineNumbers, startLineNumbersAt, noTrim, autoAnimateId, ...props }: MakeProps<CodeProps, 'pre'>): JSX.Element;
export default Code;
