/// <reference types="react" />
import { MakeProps } from './BaseComponent';
export interface IFrameProps {
    height?: number;
    src: string;
    lazy?: boolean;
    preload?: boolean;
    width?: number;
}
declare function IFrame({ autoAnimateId, fragmentIndex, height, lazy, preload, src, width, ...props }: MakeProps<IFrameProps, 'iframe'>): JSX.Element;
export default IFrame;
